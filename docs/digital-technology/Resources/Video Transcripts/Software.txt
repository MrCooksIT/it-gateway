hi in this video we'll be talking about software so what is software well software also called programs or applications are programs that run on the computer they contain instructions to tell the computer exactly what to do software is how the computer does the cool things that we want it to do there are a couple types of software the first we'll be going over is user application software now user application software are programs that run on the computer that allow us users to perform cool tasks so user applications are programs that are installed onto your computer by you the user they don't come prepackaged on the computer you go out you find them you install them and then you use them so user applications enable the user to accomplish tasks by using the application things like sending a message or writing an email or writing your paper for a class these are all examples of user applications now these user applications are written by programmers but they're packaged so that other people can install it on their own computers so a programmer makes it publishes it to the world and then other people can find these applications and install them on their own computers so some examples of user applications are things like games office applications like word processors or spreadsheets web browsers are user applications and file explorers or user applications these are all things that run on your computer that allow you to do the cool things that you want to do then we have system software now system software are programs that run on the computer that help the computer perform tasks so rather than helping you perform your tasks these help the computer perform computer tasks so there's a hierarchy going on inside of your computer where certain things depend on certain other things the very first thing you need is the hardware you need the physical machine the metal and the CPU and the discs and the mouse and the printer you need that physical hardware in order to have your computer but that Hardware doesn't do anything on its own it needs instructions to know exactly how to work so installed on the hardware prepackaged on the hardware is the system software and the system software are things like the operating system and specific utilities that help the computer do its job it helps the computer access memory and helps the computer save files it helps the computer handle multiple tasks it helps the computer turn on and off these are the systems software applications that run on the hardware once you have the system software running correctly then you can start installing application software which are much higher level and they rely on the system software to do their jobs correctly but the application software are what allow you to do your cool things like play games right papers access spreadsheets access the internet all these kinds of things so this is the hierarchy going on system software runs directly on the hardware it interacts with the hardware to help the computer do its job so it is a layer between the user applications and the hardware it helps the user applications get to the hardware the user applications can't actually access the printer and the mouse and these things on its own it needs to ask the system software to send messages to these actual physical devices so the user the user does not directly interact with the system software it's all behind the scenes you are only interacting with the user applications so what are some examples of system software while some important system software programs are the boot program which loads the operating system into main memory when the computers first turned on there's the BIOS which is the basic input/output system and this manages communication between the input and output devices and the operating system and then you have device drivers for each of your devices so this is software it's a program for a specific input or output device it tells the input or output device how it should function so one very important example of system software is operating systems now operating systems make your computer usable they make your computer operate so where's the operating system live well the operating system is between the user applications and the hardware the user applications have to ask the operating system for permission to do things with the hardware if they want to save a file or they want to load a file that I want to print something these are all messages sent to the operating system and the operating system is a program that tells the hardware what to do it is one type of system software so what does the OS do well the operating system also know as the OS manages user applications it manages memory allocation so that different applications can be using different memory it manages multitasking so that you can have multiple applications running on your computer at the same time and it displays a graphical user interface so this is what allows you to interact with the computer using displays and pictures and icons rather than a text-based console it used to be that to give the computer instructions you had to literally type in commands into this little text-based console and that was all you could do now we have graphical user interfaces where you can use a mouse and click on things and drag things around and that's how we're sending messages to the computer is through this graphical interface and that is all provided by the operating system so what are some common operating systems well the big ones today are Microsoft Windows Apple Mac OS also known as OS X Linux and UNIX pretty much every computer you'll ever use has one of these four operating systems here we have the Microsoft Windows logo the Apple logo and that penguin is actually the Linux logo so in addition to these user applications and system software there's another type of software and that's simply programs so we've been calling all of this software programs and that's true but a lot of times we use the word program to refer to a small piece of code that the user writes to do a very specific task so a program is a set of specific instructions for the computer written by a programmer using a programming language it's written to perform a task but it's not necessarily meant to be installed as software on other computers you're not necessarily going to publish it to the world it's just a program that you want to run to do something very specific like visualize some music or remind you to get up at a certain time or process a bunch of data and tell you what the biggest number in your data set is things like this simple programs that the user can write now programs are written in a programming language and a programming language is a human readable way to write computer instructions so we use a language like C or Java or JavaScript to write a computer program and when we run this computer program we are giving the computer instructions based on the program that we wrote and there are so many computer programming languages there's constantly more being created every year this is a small subset of all the computer programming languages in this course will learn how to use a computer programming language to write our own programs will actually make our own software so that is an overview of computer software